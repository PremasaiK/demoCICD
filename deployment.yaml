apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubernetes-101-v4
  labels:
    app: kubernetes-101-v4
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubernetes-101-v4
  template:
    metadata:
      labels:
        app: kubernetes-101-v4
    spec:
      containers:
        - name: kubernetes-101-v4
          image: premasaik/kubernetes-101:v4
          ports:
            - name: http
              containerPort: 3000
