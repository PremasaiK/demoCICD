apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubernetes-101-v5
  labels:
    app: kubernetes-101-v5
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubernetes-101-v5
  template:
    metadata:
      labels:
        app: kubernetes-101-v5
    spec:
      containers:
        - name: kubernetes-101-v5
          image: premasaik/kubernetes-101:v4
          ports:
            - name: http
              containerPort: 3000
